<% projects.each do |project| %>
  <%= link_to front_project_path(project), class: "project-link" do %>
    <%= image_tag project.image.url(:thumb) %>
    <%= content_tag :p, project.name, class: "project-link-name" %>
  <% end %>
<% end %>